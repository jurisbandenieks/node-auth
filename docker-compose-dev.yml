services:
  redis:
    image: "redis:latest"
  server:
    depends_on:
      - redis
    build:
      dockerfile: Dockerfile.dev
      context: ./
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - /app/node_modules
      - ./:/app
    ports:
      - "3000:3000"
